# Path-based exclusions for scripts/check-secrets.sh content scans.
# Keep this list minimal and specific; do not add broad globs.
#
# Every exclusion MUST have a matching record in
# .secret-scan-exclusions-justifications.csv with an owner, reviewed date,
# and explicit false-positive rationale.
Gemfile.lock
yarn.lock
ios/Podfile.lock
android/gradle/wrapper/gradle-wrapper.jar
