# Path-based exclusions for scripts/check-secrets.sh content scans.
# Keep this list minimal and specific; do not add broad globs.
#
# Only validated false positives are allowed:
# - lockfiles containing long dependency checksums
# - binary wrapper artifacts already verified by security review
Gemfile.lock
yarn.lock
ios/Podfile.lock
android/gradle/wrapper/gradle-wrapper.jar
