# Lane: 1.1.3.2 Action/reducer cleanup closeout audit

## Summary

This lane is a docs-only closeout audit for `1.1.3.2.1` through `1.1.3.2.10`.

Authoritative baseline for this audit:

- Chat-reported completion: the lane brief in this turn
- Repo-observed completion: current source plus current targeted test coverage

This audit did not implement any missing fixes. It only checked whether the intended cleanup outcomes are present in the current repo, whether those outcomes are directly review-confirmed, and whether any scoped follow-on work is still required.

Verification command used for review confirmation:

`npx jest --runInBand __tests__/authStorageBoundary.test.js __tests__/journalSliceBoundary.test.js __tests__/exerciseMergeDirectoryBoundary.test.js __tests__/calendarTodoOwnershipBoundary.test.js __tests__/calendarThemeRepeatedThemeBoundary.test.js __tests__/nutritionStorageBoundary.test.js __tests__/nutritionSupplementContract.test.js __tests__/recreationSliceBoundary.test.js __tests__/reduxSharedContractBoundary.test.js __tests__/accountFlows.test.js __tests__/localDataValidation.test.js`

Verification result: `11` suites passed, `107` tests passed.

Frozen reference points used during the audit:

- reducer mount keys and persisted whitelist in `src/redux/store/store.js`
- shared Redux barrel surfaces in `src/redux/constants/index.js`, `src/redux/actions/index.js`, `src/redux/reducer/index.js`, and `src/redux/index.js`
- the calendar-owned todo seam across `src/redux/actions/calendar.js`, `src/redux/actions/todo.js`, `src/redux/reducer/todo.js`, and `src/screens/calendar/pages/calendar/Calendar.js`

## Status rubric

- `done and review-confirmed`: source matches the intended scoped contract and targeted tests directly confirm it
- `done but not fully review-confirmed`: source matches, but the remaining confirmation is indirect or incomplete
- `partially complete`: some intended cleanup landed, but material drift remains in the scoped lane radius
- `still open`: the intended scoped contract is missing or contradicted by current repo evidence

Parent-lane verdict rule used here:

- `Done`: every sublane is `done and review-confirmed`
- `Done with verification gaps`: no row is partial/open, but at least one row is only `done but not fully review-confirmed`
- `Not done`: any row is `partially complete` or `still open`

## Closeout matrix

| Lane ID | Lane name | Intended focus | Intended file radius | Repo evidence observed | Status | Gap or mismatch | Recommended next action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `1.1.3.2.1` | Auth slice contract cleanup | Verify the local-only auth action/reducer contract, including profile persistence, scoped logout, full local reset, and derived user metrics, without changing store topology. | `src/redux/actions/auth.js`; `src/redux/reducer/auth.js`; `__tests__/authStorageBoundary.test.js`; `__tests__/accountFlows.test.js`; auth sections of `__tests__/localDataValidation.test.js` | Chat-reported: auth cleanup should close on the current local-profile contract rather than reopen broader store or platform decisions.<br>Repo-observed: `auth.js` keeps thin action helpers around `SET_USER`, `CLEAR_USER`, and `RESET_APP`; `logout()` still clears scoped auth keys only; `deleteAccount()` still dispatches `RESET_APP`, clears AsyncStorage/MMKV, and rehydrates bundled plans; `authReducer` still derives BMI/BMR on `SET_USER`.<br>Reviewability: direct tests cover stored profile helpers, `loggedIn`, `profile`, `changeEmail`, `changePassword`, `resetPassword`, `logout`, `deleteAccount`, relaunch persistence, upgrade readability, and reducer metric derivation. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.2` | Journal slice cleanup | Verify the current journal slice shape and reducer-owned same-day lookup/add/edit behavior without widening the slice contract. | `src/redux/actions/journal.js`; `src/redux/reducer/journal.js`; `__tests__/journalSliceBoundary.test.js`; journal coverage in `__tests__/localDataValidation.test.js` | Chat-reported: journal cleanup should preserve the current lightweight action surface and reducer-owned lookup/edit contract.<br>Repo-observed: `journal.js` is reduced to traits storage read plus dispatch wrappers; `journalReducer` still owns `allJournalEntriesList`, `allEntries`, same-day lookup by normalized date, generated ids on add, and shallow merge edits with no-op behavior on missing ids.<br>Reviewability: direct tests cover initial slice shape, same-day match/miss lookup, add/edit/no-op reducer behavior, `getTraits`, `getAllJournalEntries`, `getJournalEntries`, `addJournalEntry`, and `editJournalEntry`. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.3` | Exercise merge/directory cleanup | Verify the custom-exercise, directory-read, and merge contract, including filtered bundled directory behavior and merged picker outputs. | `src/redux/actions/exercise.js`; `src/redux/reducer/exercise.js`; `__tests__/exerciseMergeDirectoryBoundary.test.js`; exercise CRUD coverage in `__tests__/localDataValidation.test.js` | Chat-reported: exercise cleanup should preserve the current merge/directory boundary instead of redesigning the slice.<br>Repo-observed: `exercise.js` still reads `exercises` and `exercise_directory`, `deleteExercise()` still dispatches delete then merge, and `mergeExercises()` remains dispatch-only; `exerciseReducer` still filters Brunch Body directory entries, rebuilds `allExercises`, and preserves `wholeExercises` from custom exercises plus bundled resource directory data.<br>Reviewability: direct tests cover storage reads/fallbacks, directory filtering, `MERGE_EXERCISES` output, delete-plus-merge dispatch order, merge return contract, and reducer add/edit/delete behavior. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.4` | Todo/calendar ownership seam cleanup | Verify that the calendar UI keeps calendar-facing todo action names while storage, reducer ownership, persisted mount key, and state selection stay on the legacy `todo` seam. | `src/redux/actions/calendar.js`; `src/redux/actions/todo.js`; `src/redux/reducer/todo.js`; `src/redux/store/store.js`; `src/screens/calendar/pages/calendar/Calendar.js`; `__tests__/calendarTodoOwnershipBoundary.test.js` | Chat-reported: this cleanup should preserve the existing calendar-owned UI seam rather than migrate todo state ownership.<br>Repo-observed: `calendar.js` still re-exports todo thunks as `addCalendarTodoTask`, `editCalendarTodoTask`, `deleteCalendarTodoTask`, and `getCalendarTodoTasks`; `store.js` still mounts and persists the slice as `todo`; `Calendar.js` still selects `state.todo?.todoTasks` and dispatches the calendar-facing alias props; `todoReducer` still owns the CRUD state shape.<br>Reviewability: direct tests assert alias identity, shared actions barrel exposure, `todos` storage key reads, reducer shape/CRUD behavior, frozen store mount/whitelist usage, and the calendar screen seam. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.5` | Calendar theme/repeated-theme cleanup | Verify the theme and repeated-theme action/reducer contract, including recompute flow, preserved slice shape, and current recurrence semantics. | `src/redux/actions/calendar.js`; `src/redux/reducer/calendar.js`; `__tests__/calendarThemeRepeatedThemeBoundary.test.js`; theme CRUD coverage in `__tests__/localDataValidation.test.js` | Chat-reported: calendar cleanup should close on the current theme/repeated-theme contract, not reopen calendar behavior design.<br>Repo-observed: calendar actions still separate direct theme mutations from repeated-theme recompute, `getThemes()` still reads storage then triggers repeated-theme recompute, and `calendarReducer` still preserves `theme`, `themes`, `currentTheme`, `themesWithFrequency`, `repeatedTheme`, `userRepeatedThemes`, and `clearedThemeDays` plus daily/weekly/biweekly/monthly/never recurrence logic.<br>Reviewability: direct tests cover storage read and recompute chaining, simple thunk contracts, repeated-theme add/edit recompute order, initial shape, theme CRUD, clear behavior, overlap precedence, and recurrence output for all supported frequency modes. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.6` | Nutrition meals + meal-items cleanup | Verify the meals branch and meal-item selection/mutation contract, including the split between `meals` and `mealItems`. | `src/redux/actions/nutrition.js`; `src/redux/reducer/nutrition.js`; `__tests__/nutritionStorageBoundary.test.js`; meal-item coverage in `__tests__/localDataValidation.test.js` | Chat-reported: meals cleanup should preserve the existing local-storage and reducer contract rather than redesign nutrition state shape.<br>Repo-observed: `nutrition.js` still exposes thin `getMeals`, `getMealItems`, `setMealItems`, `addMealItems`, `editMealItem`, and `deleteMealItem` wrappers; `nutritionReducer` still keeps separate `meals` and `mealItems` branches, generates ids on add, mirrors nested item updates back into the owning meal, and uses replace semantics for edited meal items.<br>Reviewability: direct tests cover `getMeals` storage reads/fallbacks plus meal initial shape, add/get/set/edit/delete meal-item behavior, mirrored `meals[0].items` updates, and delete behavior. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.7` | Nutrition supplements + directory-read cleanup | Verify the supplements branch plus the legacy nutrition directory-read contract without widening nutrition cleanup beyond the scoped surface. | `src/redux/actions/nutrition.js`; `src/redux/reducer/nutrition.js`; `__tests__/nutritionSupplementContract.test.js`; relevant storage coverage in `__tests__/nutritionStorageBoundary.test.js` | Chat-reported: supplements cleanup should preserve the supplement contract and directory-read seams already in scope.<br>Repo-observed: `nutrition.js` still keeps `getSupplements`, supplement item thunks, `getMealsDirectory`, and `getMealCategories`; `nutritionReducer` still keeps `supplements`, `supplementItems`, `mealsDirectory`, and `mealCategories` in the legacy split state shape.<br>Reviewability: direct tests cover supplement storage reads/fallbacks, `meals_directory` and `meal_categories` reads/fallbacks, supplement initial shape, add/set/get/edit/delete item behavior, mirrored nested updates, and delete behavior. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.8` | Recreation routines/custom-plans/week-plan cleanup | Verify the routines, custom-plans, and week-plan contract, including the current no-storage `GET_CUSTOM_PLANS` seam and week re-selection flow. | `src/redux/actions/recreation.js`; `src/redux/reducer/recreation.js`; routines/custom-plan/week-plan sections of `__tests__/recreationSliceBoundary.test.js` | Chat-reported: this cleanup should preserve the current local recreation seam rather than redesign plan storage or state ownership.<br>Repo-observed: `recreation.js` still keeps routines and custom-plan thunks thin, `getCustomPlans()` still dispatches an empty array without adding a storage read, and `addWeekPlan()` / `editWeekPlan()` still re-dispatch `GET_WEEK_PLAN`; `recreationReducer` still keeps `routines`, `routineTasks`, `customPlans`, and the shared `weekPlan` field with the current intermediate array-to-selected-item behavior.<br>Reviewability: direct tests cover initial slice shape, routine storage reads/fallbacks, routine add/get/add-item/edit-item/delete-item/delete behavior, custom-plan generated ids and empty week arrays, `GET_CUSTOM_PLANS` no-storage behavior, and `GET_WEEK_PLAN` / `ADD_WEEK_PLAN` / `EDIT_WEEK_PLAN` semantics. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.9` | Recreation workouts/catalog cleanup | Verify the workout CRUD and Brunch Body catalog contract, including MMKV-backed catalog reads and the shared week-plan write path. | `src/redux/actions/recreation.js`; `src/redux/reducer/recreation.js`; workouts/catalog sections of `__tests__/recreationSliceBoundary.test.js`; workout CRUD coverage in `__tests__/localDataValidation.test.js` | Chat-reported: this cleanup should preserve the current workouts/catalog branch and not reopen adjacent recreation architecture decisions.<br>Repo-observed: `recreation.js` still reads Brunch Body plans from MMKV, resolves Brunch Body week selection by legacy id-or-name lookup, reads workouts from AsyncStorage, and keeps thin workout CRUD thunks; `recreationReducer` still preserves `brunchBodyPlans`, `workouts`, `completedWorkouts`, and writes catalog week selection into the shared `weekPlan` field.<br>Reviewability: direct tests cover MMKV plan reads/fallbacks, missing-plan warn/null behavior, `GET_BRUNCH_BODY_WEEK_PLAN`, workout storage reads/fallbacks, workout add/edit/delete semantics, and completed-workout append behavior. | `done and review-confirmed` | None. | Close sublane with no follow-on. |
| `1.1.3.2.10` | Shared Redux contract cleanup | Verify the frozen shared Redux import/export surface and root store contract without reopening reducer mount or persistence decisions. | `src/redux/constants/constants.js`; `src/redux/constants/index.js`; `src/redux/actions/index.js`; `src/redux/reducer/index.js`; `src/redux/index.js`; `src/redux/store/store.js`; `__tests__/reduxSharedContractBoundary.test.js` | Chat-reported: shared Redux cleanup should end with the current constants/actions/reducers/root barrel contract and frozen store keys intact.<br>Repo-observed: `constants/constants.js` still exposes the current domain contracts including `RESET_APP`; `constants/index.js`, `actions/index.js`, `reducer/index.js`, and `redux/index.js` still preserve the shared barrel surfaces; `store.js` still mounts and whitelists `auth`, `recreation`, `journal`, `nutrition`, `calendar`, `exercise`, and `todo` under the root persist boundary.<br>Reviewability: direct tests cover full constants barrel parity, action barrel union, reducer barrel aliases, root Redux namespace exports, and the frozen store mount/whitelist contract including `RESET_APP`. | `done and review-confirmed` | None. | Close sublane with no follow-on. |

## Parent verdict

Verdict for parent lane `1.1.3.2`: `Done`

Why:

- all ten audited sublanes are `done and review-confirmed`
- no row is `done but not fully review-confirmed`
- no row is `partially complete`
- no row is `still open`
- the frozen store mount keys, persisted whitelist, and shared Redux import/export surface remain intact in the current repo
- the targeted verification run passed in full

## Unresolved items

None within the scoped `1.1.3.2.x` action/reducer cleanup audit.

No follow-on cleanup lane is required from this closeout audit. Preserved legacy seams that remain in place, such as the `todo` persisted slice key, `GET_CUSTOM_PLANS` returning an empty array, and the shared `weekPlan` field for recreation, are directly confirmed current contracts rather than unresolved gaps in this lane.
